import { CapitalizableDirective } from './capitalizable.directive';

describe('CapitalizableDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalizableDirective();
    expect(directive).toBeTruthy();
  });
});
